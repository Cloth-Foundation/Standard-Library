// Part of the Cloth Standard Library project, under the Apache License v2.0
// with LLVM Exceptions. See LICENSE.txt in the project root for license
// information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

sealed class: FloatingPoint {
  final float64 value;

  Float64 (float64 value): FloatingPoint() {
    self.value = value;
  }

  override func Equals (Object? other): bool {
    return __valueBoxEquals(self, other);
  }

  override func HashCode (): uint64 {
    return __valueBoxHashCode(self);
  }

  override func ToString (): string {
    return __valueBoxToString(self);
  }
}
